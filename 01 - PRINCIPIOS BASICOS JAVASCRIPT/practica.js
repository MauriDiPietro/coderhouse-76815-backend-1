class ProductManager {
    constructor() {
        this.products = []
    }

    getProducts(){}

    validateProduct(prod){
        // if(!prod.title || !prod.description || !prod.price || !prod.thumbnail || !prod.code)
    }

    addProduct(prod){
        
    }

    getProduyctById(){}

}

const productManager = new ProductManager()

productManager.addProduct({ title: 'Product1', description: '' })